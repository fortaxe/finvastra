import FeaturesSection from "@/components/ui/features-section";
import { wealthFeatures } from "./wealth-features-data";

const WealthFeatures = () => {
  return <FeaturesSection features={wealthFeatures} />;
};

export default WealthFeatures;

